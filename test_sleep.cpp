#include <iostream>
#include <thread>

int main() {
    /*
    std::cout << "1" << std::endl;
    std::this_thread::sleep_for(std::chrono::seconds(10));
    std::cout << "2" << std::endl;
    */
    while (true);
}
